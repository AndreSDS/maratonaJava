public class Operadores {
    public static void main(String[] args) {

        /*
        ++ incremento
        -- decremento
        + soma que também concatena
        -subtração
        / divisão
        * multiplicação
        () cast
        % resto da divisão
        ! negação
        < menor que
        > maior que
        <= menor igual
        >= maior que
        == igualdade
        != diferente
         */

        int numero1 = 10;
        int numero2 = 20;
        //int soma = numero1+numero2;

        System.out.println("A soma é "+(numero1+numero2));
    }
}
